{
  "name": "cowell-ocr",
  "version": "1.0.0",
  "private": true,
  "description": "Cowell OCR — LED field survey digitization platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
